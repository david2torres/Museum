export enum ProgresssBarClass_Enum {
  Weak = 'progress-bar weak',
  medium = 'progress-bar medium',
  strong = 'progress-bar strong',
  default = 'progress-bar',
}

export enum WidthProgress_Enum {
  default = 0,
  Weak = 33.33,
  medium = 66.66,
  strong = 100,
}
